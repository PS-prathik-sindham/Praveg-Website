# Copy this file to `.env.local` and fill in the values, then restart the dev server
# (env vars are only read at startup).

# Contact form -> Microsoft Power Automate flow that writes a row to Excel and
# emails the sales inbox. Paste the "When a HTTP request is received" trigger URL.
CONTACT_WEBHOOK_URL=
